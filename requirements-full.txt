# Optional upgrades. Each is detected automatically when present.

langgraph>=0.0.40          # replaces SimpleGraph with a compiled state machine
langchain-core>=0.1        # message types used by langgraph
mlflow>=2.10               # replaces the JSONL run log

# Serving
fastapi>=0.110
uvicorn[standard]>=0.27
streamlit>=1.30
pydantic>=2.0

# Live collectors (see README "Pointing it at a real deployment")
apache-airflow-client>=2.8 # Airflow REST API client
boto3>=1.34                # S3 log retrieval

# Dev
pytest>=7.4
